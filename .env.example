DJANGO_DEBUG=True
DJANGO_SECRET_KEY=dev-secret-change-me
ALLOWED_HOSTS=127.0.0.1,localhost

# DB: 先用 sqlite；要用 Postgres 時把 DB_ENGINE 改成 postgres 並填以下
DB_ENGINE=sqlite
POSTGRES_DB=oj
POSTGRES_USER=ojuser
POSTGRES_PASSWORD=ojpass
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432

# 前端本機網址（CORS）
CORS_ALLOWED_ORIGINS=http://localhost:5173

#MOSS ID(請寄信去MOSS官方信箱申請你的moss user id "moss@moss.stanford.edu")
MOSS_USER_ID=your-moss-user-id

# ====================
# Celery 配置
# ====================
# Celery Broker (使用 Redis database 0)
CELERY_BROKER_URL=redis://127.0.0.1:6379/0
# Celery Result Backend (使用 Redis database 0)
CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/0

# ====================
# Sandbox 配置
# ====================
# Sandbox API 位址
SANDBOX_API_URL=http://34.81.90.111:8000
# Sandbox API 請求超時（秒）
SANDBOX_TIMEOUT=30
# Sandbox API Key（用於認證 Sandbox API）
SANDBOX_API_KEY=happylittle7
# Sandbox Callback URL（Sandbox 判題完成後會打回來的 URL）
# 注意：本地開發時需要用 ngrok 等工具暴露到公網，或者先不設定（暫時不處理 callback）
# SANDBOX_CALLBACK_URL=http://localhost:8000/api/submission/callback/
